# q
https://xenospectrum.com/openai-codex-cli-ssd-wear-bug/

## raw
OpenAI Codex CLIの不具合が引き起こすSSD寿命の深刻な短縮：年間640TBの過剰書き込みとその機構
Y Kobayashi
2026年6月24日
約 9 分
862 閲覧
OpenAI Codex CLIの不具合が引き起こすSSD寿命の深刻な短縮：年間640TBの過剰書き込みとその機構
この記事のポイント

    何が起きた: OpenAI Codex CLIのログ出力設定の不備により、ユーザーのローカルSSDに対して年間推計約640TBもの異常なデータの書き込みが発生した。
    なぜ重要か: 一定の書き込み量（TBW）で寿命を迎えるSSDの物理的な耐久性を数ヶ月で使い潰し、ストレージ換装が不可能なノートPCでは端末全体の寿命を終わらせる損害に直結する。
    次に見るべき点: OpenAIが適用した暫定パッチによる書き込み削減の実際の効果と、常駐型AIエージェントがハードウェアに与える負荷の監査体制の有無。

OpenAIが提供するコーディングエージェント「Codex CLI」のバックグラウンド処理において、ユーザーのローカルストレージに対して異常な規模のデータ書き込みが継続的に行われている事実が明らかになった。事の発端は、Apache Flinkのプロジェクト管理委員会メンバーであるRui FanがGitHubに報告したIssue #28224である。この報告によれば、Codex CLIを稼働させたマシンで21日間の連続起動を行った結果、メインのSSDに対して約37TBという膨大なデータの書き込みが記録されていた。

このペースを1年間に換算すると、約640TBの書き込み量に達する。一般に流通している1TBクラスのコンシューマー向けSSDの耐久性（TBW：Total Bytes Written）は、Samsung 9100 PROなどを例にとっても600TB程度に設定されている。つまり、このCodex CLIの不具合は、一般的なSSDの製品寿命として想定された書き込み可能領域を、わずか1年未満で完全に食いつぶすことを意味する。

SSDはフラッシュメモリの特性上、データを書き込むたびにセルが物理的に劣化し、TBWの制限を超過すると性能の低下や突然のデータ消失を招く。この過剰な書き込みは、ユーザーのストレージの経済的価値を著しく損なう。Rui Fanの例では、SSDのTBW単価（ドライブ価格を保証TBWで割った値）に基づく試算で、37TBの無駄な書き込みが約12ドル分のストレージ寿命を消費したと見積もられている。開発コミュニティ全体で見れば、この不具合が放置された期間中に数百万ドル規模のハードウェア寿命が不必要に削られたという推計も提示されている。
過剰書き込みを引き起こすSQLiteとWALの書き込み増幅機構

この不具合が深刻なのは、標準的な監視ツールではディスクスペースの圧迫として検知されにくい点にある。Codex CLIはローカルに「logs_2.sqlite」というSQLiteデータベースを作成し、そこへログを保存する。しかし、このデータベースは挿入された古い行を高速に削除（Prune）し続けるため、論理的なファイルサイズ自体は数百メガバイト程度からほとんど増加しない。Issueの検証でも、15秒間に3万6,211行が挿入されたにもかかわらず、データベース全体の行数は約68万行でほぼ横ばいを維持していた。「du」コマンドなどでファイルサイズを監視していても、異常は表面化しない。

ファイルサイズが増加しない背後で、SQLiteのWrite-Ahead Logging（WAL）モードによる書き込み増幅（Write Amplification）がストレージを削り続けている。WALモードでは、すべてのデータ変更がまず独立したジャーナルファイル（-wal）に追記され、その後のチェックポイント処理でメインデータベースへマージされる。Codex CLIのログ処理では1分間に数万回もの挿入と削除のサイクルが発生し、これに伴う頻繁なチェックポイント処理がフラッシュメモリへの物理的な書き込みを激しく繰り返す。

論理的なデータ量は一定に保たれていても、物理的なフラッシュセルへの書き込みは数倍に膨れ上がる。この書き込み増幅こそが、ディスク容量を圧迫せずにSSDの寿命だけを静かに奪うメカニズムである。ユーザーがこの物理的書き込み量を正確に把握するには、論理ファイルサイズではなく、Linux環境であれば「smartctl」コマンド等を用いてNVMeドライブなら「Data Units Written」、SATAドライブなら「Total_LBAs_Written」といったSSD自体のSMARTデータを直接読み取る以外に方法はない。
制御不能なTRACEログと開発側の実装不備

異常な量のログが生成される原因は、Codex CLI内部のロガー設定における初歩的かつ致命的な欠陥に起因する。該当のSQLiteフィードバックログは、初期設定で「Level::TRACE」に固定されている。TRACEレベルはログ出力のなかで最も冗長な設定であり、通常は深いデバッグ作業時にのみ有効化されるべき機能である。Codexの出力には、生のWebSocketやSSEのペイロード、「passwd」や「ld.so.cache」といったOSレベルのファイルアクセスイベント、さらには「tokio-tungstenite」や「hyper_util」といった内部の依存ライブラリから発生する細かな状態変化まで、すべてが無差別に記録されている。

分析によると、出力されるログの70.7%はこのTRACEレベルの記述であり、これにOpenTelemetryのミラーイベント（25.3%）を加えると、全書き込み量の約96%を占める。これらのデータの大半は、一般的なユーザー環境において診断用途としての実質的な価値を持たない。製品版のソフトウェアにおいてこのような過剰なログを常時出力する設定は、本番環境への配備基準を大きく逸脱している。

問題を悪化させているのは、このロガーがRustアプリケーションにおける標準的なログレベル制御変数である「RUST_LOG」を完全にバイパスしている事実である。ユーザー側で環境変数を「RUST_LOG=warn」などに設定し、出力を警告以上に絞ろうと試みても、CodexのSQLiteロガーはそれを無視してTRACEレベルでの記録を続行する。ユーザーの手元に制御を委ねず、最高レベルの冗長性でデータを吐き出し続ける仕様が、被害を回避不可能にしていた。

これらの不自然な挙動は、突発的に生じたものではない。4月10日の時点ですでにIssue #17320として、RUST_LOGを無視するTRACEログがストリーミング中に過剰なWAL書き込みを発生させる問題が報告されていた。その後も複数の報告が上がっていたにもかかわらず、数ヶ月にわたって根本的な修正が提供されなかった事実は、開発プロセスの盲点を示している。
XenoSpectrum サポーター

広告なし・ブックマーク無制限で読めるサポーター制度。ご支援は、広告に左右されない独立した編集につながります。
¥580/月〜
ユーザー側の暫定対応と修正状況

OpenAIからの公式な修正が提供されない中、LinuxおよびmacOSのユーザーコミュニティは自衛のための暫定的な回避策を編み出した。それは、問題のSQLiteファイル（~/.codex/logs_2.sqlite）をRAMディスクである「/tmp」へシンボリックリンクするという手法である。「/tmp」は物理的なフラッシュストレージではなく揮発性のメモリ上に展開されるため、どれだけ書き込みが発生してもSSDの寿命を消耗しない。このログファイルには復元すべき会話履歴やユーザーデータは含まれておらず、再起動時に消去されても実用上の不都合は生じない。

別の回避策として、SQLiteのトリガーを作成してログテーブルへのINSERT処理をブロックし、書き込み自体を物理的に無効化する手法や、既存のファイルに対して「VACUUM」コマンドを実行して数十GBに膨れ上がった不要領域を解放する手法も共有されている。しかし、Windows環境（WSL2を含む）のユーザーにとっては、これらと同等の容易な回避策は確立されていない。タスクマネージャー上でディスク使用率が100%に張り付く現象が報告される中、根本的な解決を待つしかない状況が続いた。

事態が動いたのは6月22日である。OpenAIの開発チームは関連するIssueに対応し、不要なログの出力を大幅に削減する修正パッチを反映したアップデートを適用した。ユーザーはCodex CLIを最新版へアップデートした上で、肥大化した「logs_2.sqlite」ファイルを手動で削除することが推奨されている。起動時に新しくクリーンなデータベースが作成されることで、ようやくSSDへの過剰な負荷は正常な範囲に収まる。
常駐型AIエージェントがもたらす新たなハードウェアリスク

一連の騒動は、AI開発ツールの進化がもたらす新しい種類のリスクを顕在化させた。従来のソフトウェアとは異なり、Codex CLIのようなAIエージェントはバックグラウンドで常に稼働し続ける。さらに4月のアップデートではmacOS環境でのフルデスクトップ操作権限が付与され、ユーザーの画面を定期的にキャプチャする機能が導入されるなど、エージェントがローカルシステムにアクセスする権限は急速に拡大している。

高い権限を持ったプロセスが常に背後で動く環境において、ログ出力のバグという一見些細な実装のミスは、ユーザーが気づかないうちに物理的ハードウェアを不可逆的に損傷させる凶器に変わる。とくに近年の薄型ノートPCの多くは、ストレージがマザーボードに直接はんだ付けされている。SSDが寿命を迎えることは、数千円の部品交換で済む問題ではなく、端末全体の死を意味する。さらに、データベースのサイズが200MBを超えると30分から1時間程度でCodex CLI自体がクラッシュし始めるという二次的な障害も報告されており、ストレージの圧迫はソフトウェアの動作安定性をも破壊する。

AIエージェントの実装においては、生成されるコードの質や推論の精度はもちろん、ホストマシンのリソースを安全に扱うための品質管理も同時に問われる。ローカル環境で常時稼働するツールが急速に普及する現在、ソフトウェアの動作がハードウェアの寿命という物理的な制約に直結しているという事実を、開発側もユーザー側も強く意識する必要がある。

## q
- 具体的にどういう不具合？
- ログ書き込みがssdの寿命に影響するって軽微で無視できると思ってるんだけど、そうでもないの？

# a

記事の内容ベースで言うと、不具合の本質はかなり単純です。

## 1. 具体的にどういう不具合？

要するに、

> Codex CLI がローカルの SQLite ログDBに、通常運用では不要な超詳細ログを大量に書き続けていた

という問題です。

もう少し分解すると、主に以下の合わせ技です。

### 問題1：ログレベルが `TRACE` 相当で出続けていた

`TRACE` はログレベルの中でも最も細かいものです。

普通は、

- 開発者がバグ調査するときだけ有効化する
- 本番版・通常利用では `INFO` や `WARN` 程度に抑える

べきものです。

ところが記事の説明では、Codex CLI のローカルログがデフォルトで極めて冗長に出ていたようです。

たとえば、

- WebSocket / SSE の細かい通信イベント
- 内部ライブラリの状態変化
- ファイルアクセスイベント
- OpenTelemetry 系のミラーイベント

などが大量に記録されていた、という話です。

つまり「ユーザーにとってほぼ役に立たない内部デバッグログ」を、常時、ローカルSSDに書き込んでいた。

---

### 問題2：ユーザーがログレベルを下げられなかった

Rust製アプリでは一般に、

```bash
RUST_LOG=warn
```

のようにしてログ出力を抑えることがあります。

しかし記事によると、Codex CLI の該当SQLiteロガーはこの設定を無視していた。

つまりユーザーが、

```bash
RUST_LOG=warn
codex ...
```

のようにしても、SQLiteへのTRACEログ書き込みは止まらなかった。

これはかなり悪いです。  
「うるさいログが出る」だけでなく、「止める手段がない」ということなので。

---

### 問題3：SQLite + WAL により、ファイルサイズ以上の物理書き込みが発生した

ここが重要です。

Codex CLI は `logs_2.sqlite` のような SQLite DB にログを書いていた。  
ただし、古いログを削除する prune 処理も走っていたため、ファイルサイズ自体はそこまで増えない。

見た目としては、

```bash
du -h ~/.codex/logs_2.sqlite
```

しても、数百MB程度で止まって見える。

なのでユーザーは、

> まあログDBが少しあるだけか

と思ってしまう。

しかし実際には裏で、

1. 大量INSERT
2. 古い行をDELETE
3. SQLite WALファイルへ追記
4. checkpointで本体DBへ反映
5. またINSERT
6. またDELETE

という処理が繰り返される。

その結果、**DBファイルの見た目のサイズは増えないのに、SSDには大量の書き込みが発生する**。

これが今回の肝です。

---

## 2. 「ログ書き込みがSSD寿命に影響」って軽微では？

普通のログ書き込みなら、ほぼ無視していいです。

たとえば一般的なアプリが、

- 1日数MB
- 1日数十MB
- 多くても数百MB

くらいのログを書く程度なら、SSD寿命への影響はほぼ気にしなくていいです。

しかし今回の記事にある数字が正しいなら、これは普通のログではありません。

報告値は、

> 21日で約37TB書き込み

です。

これは1日あたりにすると、

```text
37TB / 21日 ≒ 1.76TB/日
```

です。

1年換算では、

```text
1.76TB/日 × 365日 ≒ 642TB/年
```

になります。

これはかなり大きいです。

---

## 3. どれくらい深刻なのか

一般的なコンシューマ向けSSDのTBWは、ざっくり以下のような感じです。

| SSD容量 | 典型的なTBW目安 |
|---|---:|
| 500GB | 150〜300TBW |
| 1TB | 300〜600TBW |
| 2TB | 600〜1200TBW |
| 4TB | 1200〜2400TBW |

もちろん製品によって違います。

もし1TB SSDで600TBWの製品なら、今回のように年間640TB書かれると、

> その不具合だけで1年以内に保証TBW相当を使い切る

というレベルです。

これは「軽微」とは言いにくいです。

---

## 4. ただし、TBWを超えた瞬間にSSDが死ぬわけではない

ここは誤解しない方がいいです。

TBWは主にメーカー保証上の耐久目安です。

600TBWのSSDに601TB書いた瞬間に壊れる、という意味ではありません。  
実際にはTBWの数倍書いても動く個体はあります。

ただし、

- NANDセルの劣化は確実に進む
- 書き込み速度低下の可能性が上がる
- 不良ブロック増加の可能性が上がる
- データ保持期間が短くなる可能性がある
- ノートPC内蔵・はんだ付けSSDだと交換が困難

という問題があります。

なので正確には、

> すぐ壊れるとは限らないが、明確に寿命を削る。しかも今回の規模なら無視できない

です。

---

## 5. 「ログだから大したことない」と思いやすい理由

普通はその感覚で正しいです。

普通のログは、

```text
数MB〜数百MB/日
```

程度です。

仮に1日1GBログを書いても、

```text
365GB/年 = 0.365TB/年
```

です。

600TBWのSSDなら誤差みたいなものです。

しかし今回の報告値は、

```text
約1.76TB/日
```

です。

1日1GBどころか、**1日約1760GB**。

普通のログとは桁が違います。

平均書き込み速度に直すと、

```text
37TB / 21日 ≒ 20MB/s
```

くらいの継続書き込みです。

20MB/sというと一瞬なら大したことありませんが、24時間ずっと続くと巨大です。

---

## 6. SSD寿命に効くのは「ファイルサイズ」ではなく「総書き込み量」

ここも重要です。

ユーザーがよく見るのは、

```bash
du -h
df -h
```

などの容量です。

しかしSSD寿命に効くのは、

> いまファイルが何GBあるか

ではなく、

> これまでSSDに何TB書いたか

です。

たとえば、同じ200MBのSQLiteファイルでも、

- 200MBを一度だけ書いた
- 200MBの範囲を毎秒何度も書き換え続けた

ではSSDへの負荷は全然違います。

今回のようなSQLite + WAL + INSERT/DELETEループでは、後者になりやすいです。

---

## 7. 見るべき指標

確認するなら、ファイルサイズではなくSSDのSMART値を見るべきです。

NVMe SSDならだいたい以下の項目です。

```text
Data Units Written
```

Linuxなら例：

```bash
sudo smartctl -a /dev/nvme0
```

または：

```bash
sudo nvme smart-log /dev/nvme0
```

NVMeの `Data Units Written` は通常、1 unit = 512,000 bytes です。

概算は：

```text
Data Units Written × 512,000 / 1,000,000,000,000 = TB
```

SATA SSDなら、

```text
Total_LBAs_Written
Host Writes
Total NAND Writes
```

などの名前で出ることがあります。

Windowsなら CrystalDiskInfo などで「総書込量」「Host Writes」を見るのが簡単です。

---

## 8. 今回の不具合の悪質さ

今回の問題が厄介なのは、単に「ログが多い」だけではなく、

- デフォルトでTRACE級ログ
- ユーザーが環境変数で抑えられない
- SQLite WALにより物理書き込みが増幅
- pruneによりファイルサイズは増えにくい
- そのため異常に気づきにくい
- 常駐型ツールなので長時間続く

という点です。

つまり、ユーザーから見ると、

> ディスク容量は減っていない  
> でもSSDの総書き込み量だけが増えている

という状態になります。

これは普通の「ログファイルが肥大化した」問題より厄介です。

---

## 9. ただし、記事中の `/tmp` 回避策には注意

記事では `/tmp` にシンボリックリンクすればRAM上になる、という説明がありますが、これは環境によります。

Linuxでも `/tmp` が必ずRAMディスクとは限りません。  
macOSの `/tmp` も通常はディスク上です。  
WSL2の `/tmp` も仮想ディスク経由で結局SSDに書かれる可能性があります。

本当にRAMに逃がすなら、Linuxでは例えば `/dev/shm` や明示的に `tmpfs` マウントした場所を使う必要があります。

ただし、現実的にはまず最新版へアップデートするのが先です。

---

## 10. 結論

質問への答えはこうです。

### 具体的な不具合は？

Codex CLIがローカルSQLite DBに、通常不要なTRACE級ログを大量に書き続けていた。  
さらにそれをユーザーが `RUST_LOG` などで抑制できず、SQLite WALの仕組みでファイルサイズ以上の物理書き込みがSSDに発生していた、という不具合です。

### SSD寿命への影響は無視できる？

普通のログなら無視できます。  
しかし今回のように、

```text
21日で37TB
年間換算で約640TB
```

という規模なら無視できません。

1TBクラスの一般的なSSDなら、保証TBW相当を1年以内に使い切る可能性があります。

ただし、TBWを超えたら即死するわけではありません。  
とはいえ、SSDの劣化を確実に進めるレベルなので、「軽微」とは言えないです。
